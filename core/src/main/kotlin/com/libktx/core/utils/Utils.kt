package com.libktx.core.utils

enum class SoundAssets(val path: String) {
    Drop("sounds/drop.wav")
}

enum class MusicAssets(val path: String) {
    Rain("music/rain.mp3")
}

enum class ImageAssets(val path: String) {
    Atlas("images/simpleGame.atlas")
}